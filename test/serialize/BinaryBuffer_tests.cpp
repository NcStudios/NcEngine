#include "gtest/gtest.h"
#include "serialize/BinaryBuffer.h"

#include <algorithm>

using namespace nc::serialize;









TEST(BinaryBuffer, ToFile)
{
    // auto buffer = nc::serialize::BinaryBuffer{};
    // auto expected = Foo{42, "test", 100};
    // Serialize(buffer, expected);
    

    // buffer.WriteToFile("test_out");

    // auto inBuffer = nc::serialize::BinaryBuffer{"test_out"};
    // auto actual = Foo{};
    // Deserialize(inBuffer, actual);
}





