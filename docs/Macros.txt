
NC_TESTS_ENABLED
    Default false. Flag used by cmake to enable building test executables.

NC_EDITOR_ENABLED
    Default true. Must be set while building to exclude the editor/imgui
    from the final build. Editor specific code should use #ifdef checks
    on this.

NC_TRACE_V_ENALBED

TRACE_V("str literal");