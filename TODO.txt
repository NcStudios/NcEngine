+Rendering
    -load image from file(convert to some standard formatting?)
    -finish sprite component
    -need some concept of z layers and separate render passes for each layer
    -occlusion culling? - probably wait until 3D
    -multiple threads?

+Physics
    -separate collision layers
    -different collider types (at least static/dynamic & physics/trigger)
    -raycasting

    
--Renderer::Renderer not using all parameters?
--fix vector.cpp
    -rect should be moved
    -need func and operators for all vecs